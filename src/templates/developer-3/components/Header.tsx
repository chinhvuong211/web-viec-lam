import Header from '@/components/layout/Header';

export default function Developer3Header() {
    return (
        <Header
            theme={{
                text: 'text-light-primary',
                font: 'font-medium',
                contact: 'bg-transparent border-light-primary',
            }}
        />
    );
}
